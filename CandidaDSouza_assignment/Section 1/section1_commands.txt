# show all files
ls -lah

# find all .txt files
find ~ -type f -name "*.txt"

# show last 20 lines
tail -n 20 scan_results.txt

# count "OPEN" lines
grep -c "OPEN" scan_results.txt

# create folders
mkdir -p security/tools/scanners

# make file executable
chmod +x exploit.sh

# show user, system name, and location
whoami && hostname && pwd

# find recent files (last 7 days)
find . -type f -mtime -7

# search for "password"
grep -r "password" .

# show system info
uname -a
